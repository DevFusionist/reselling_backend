openapi: 3.0.0
info:
  title: Reseller API
  version: "1.0.0"
paths:
  /payments/create-order:
    post:
      responses:
        '200': { description: ok }
  /payments/webhook:
    post:
      responses:
        '200': { description: ok }
